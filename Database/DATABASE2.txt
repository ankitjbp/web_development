CRUD operations=>
------------------


1) SQL => Structure Query Language 

Database => tables (which stored Interrelated information) 
We will cover MYSQL. 

Syntax to login in MYSQL Database 

mysql -u root -p

to connect any database we must required these
parameters 

1)host 
2)database's name 
3)username 
4)password
5)port(optional)

Database's Structure or schema 

table -> 

student
id=> integer 
name, => text 
age, => numeric
stream=>

Syntax to create table

create table <table_name>  
(field_name1 data_type(length),
field_name2 data_type(length),
.
.
.
field_namen data_type(length) );

create table student  
(id integer(3),name varchar(100), age integer(2)stream varchar(50) );

2)NoSQL